import { AniLink } from '..';

export function Logo() {
  return (
    <AniLink className="text-2xl" variant={null} size={null} href="/">
      <span>ani</span>
      <span className="font-serif text-3xl text-purple-600">Wave</span>
    </AniLink>
  );
}
